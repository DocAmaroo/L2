#include <iostream>
#include <cstdlib>
#include "tableau-cellule.h"
#include "cellule.h"

int main(int argc, char ** argv){
  if(){
    
  }
  TableauCellule T(10);
  T.read(std::cin);
  T.write(std::cout);
  return 0;
}
