#AllInOneLine :
ls > rep && date >> rep && cat rep && rm rep && (hostname && who) > info && ls -t

#MacStuff
cat Mac.txt | grep 'Mathematica'
cat Mac.txt | grep '.fr'
cat Mac.txt | grep -c 'mac\|Mac'
cat Mac.txt | grep -E -v '.edu.*US|US.*.edu'
cat Mac.txt | grep [A-Z] Mac.txt
cat Mac.txt | grep -Ew 130.[0-9]{1,3}.[0-9]{1,3}.1 (presque ça)
